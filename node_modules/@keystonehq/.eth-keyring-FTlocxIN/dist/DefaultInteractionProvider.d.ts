import { EthSignRequest, CryptoHDKey, ETHSignature } from '@keystonehq/bc-ur-registry-eth';
import { InteractionProvider } from '@keystonehq/base-eth-keyring';
export declare class DefaultInteractionProvider implements InteractionProvider {
    private static instance;
    private keystoneSDK;
    constructor();
    readCryptoHDKey: () => Promise<CryptoHDKey>;
    requestSignature: (ethSignRequest: EthSignRequest, requestTitle?: string, requestDescription?: string) => Promise<ETHSignature>;
}
